# Floors track current releases; nothing is capped except where a cap encodes a
# real constraint. Verified on Python 3.13 against: streamlit 1.60.0,
# pandas 3.0.5, numpy 2.5.1, scipy 1.18.0, scikit-learn 1.9.0, xgboost 3.3.0,
# yfinance 1.5.2, alpaca-py 0.43.5, plotly 6.9.0.

# plotly_chart's width= replaced use_container_width, whose removal date passed.
streamlit>=1.60.0
streamlit-autorefresh>=1.0.1

pandas>=2.2
numpy>=2.0

# Used directly by train.py for the Diebold-Mariano p-value. It arrives
# transitively via scikit-learn, but the import is ours, so declare it.
scipy>=1.11

# 1.6 dropped the _estimator_type protocol — paired with xgboost 2.x this
# raises TypeError inside load_model(). See the xgboost floor below.
scikit-learn>=1.6

# Floor must stay >= the version that wrote models/ (3.2.0 — see the "version"
# field inside the model JSON). 2.x cannot be substituted: its sklearn wrapper
# still expects the _estimator_type protocol that scikit-learn dropped in 1.6,
# so with any current scikit-learn, load_model() raises before serving a single
# request. The major cap is deliberate: a 4.x is not guaranteed to read the
# committed 3.2.0 artifacts, and this app ships models rather than training on boot.
xgboost>=3.2.0,<4.0.0

yfinance>=0.2.60
alpaca-py>=0.20.0
plotly>=6.0
python-dotenv>=1.0.0
