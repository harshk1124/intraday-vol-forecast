# Environments
.venv/
venv/
env/

# Secrets — never commit real API keys
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Generated artifacts
# models/ is deliberately tracked: Streamlit Community Cloud serves only what
# is committed and offers no shell to run train.py, so an untracked models/
# leaves the deployed app permanently stuck on "train a model first".
data/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
